import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
    root: {
        marginTop: '5.5rem',
    },
    noBooks: {
        maxWidth: '50%',
        marginTop: '10rem',
        marginBottom: '5rem',
        margin: '0 auto'
    },
    categoryTitle: {
        marginTop: '1.5rem',
        marginBottom: '1rem',
        color: '#4F4F4F'
    },
    link: { textDecoration: 'none' }
});