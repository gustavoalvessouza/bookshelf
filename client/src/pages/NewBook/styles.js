import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
    root: {
        marginTop: '5.5rem',
    },
    addBookIcon: {
        maxWidth: '30%',
        marginTop: '2rem',
        marginBottom: '2rem',
        margin: '0 auto'
    },
});